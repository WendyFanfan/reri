{
  "name": "rerisnap",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd rerisnap-website && npm run dev\" \"netlify dev --port=8888 --functions=reri/functions\"",
    "build": "cd rerisnap-website && npm run build",
    "serve": "cd rerisnap-website && npm run preview"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "netlify-cli": "^16.0.0"
  }
}